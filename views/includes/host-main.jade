h3 숙소 등록
form#form-task.form-inline(class='hosts')
  .form
    .main
      input(type='text', id='content', name='content', placeholder='회원님의 숙소를 간단히 소개해주세요.')
    .sub
      .form-group
        select.form-control(name='category')
          option(value='서울', selected) 서울
          option(value='경기') 경기
          option(value='부산') 부산
          option(value='기타') 기타
      .form-group
        select.form-control(name='priority')
          option(value='1', selected) 개인실
          option(value='2') 다인실
          option(value='3') 집전체
  .actions
      button#post-task.btn.btn-primary.btn-lg.btn-block(type='button') 등록
h3 등록된 숙소 보기
ul.tasks

.no-task 
  .alert.alert-info Task가 없습니다.
script#task-template(type='text/template').
  <li class='task <%= doneStr %>' data-id='<%= id %>'>
    <span class='check'>
      <i class='fa fa-check fa-lg'></i>
    </span>
    <span class='content'><%=content %></span>
    <% if (category) { %>
      <span class='label label-default category'> 
        <%= category %>
      </span>
    <% } %>
    <span class='priority p<%= priority %>'>
      <%= (priority === 3) ? "집전체" : ((priority === 1) ? "개인실": "다인실") %>
    </span>
    <a href='#' class='remove'><i class='fa fa-trash-o'></i></a>
  </li>
      
script.
  $(function() {
    var host = new TaskController();    
  });
